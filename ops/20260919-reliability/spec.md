# September 19 relay reliability repair

## Authorized outcome
Prepare CNY 100 recharge for each of Haina and JojoCode; user payment interaction may be needed. Remove confirmed quota-failing routes from active selection until payment is verified. Preserve all account and usage records and prices. Restore patrol after reboot; diagnose recurring unsupported-model probes and add Banana b64_json compatibility if its implementation can preserve caller format.

## Acceptance
- Snapshot channel and ability rows before transaction; retain rollback SQL.
- GPT-5.6 routes to Hanhe, GPT-5.5 and GPT-6 to Code Plan, GPT-Image-2 to Maolao while exhausted sources are paused.
- Alias gpt-6 maps to gpt-6-astra on Code Plan, preserving original group restrictions.
- Patrol trigger directory is recreated by tmpfiles on boot and patrol can run.
- Do not disable ordinary user accounts to suppress errors; identify caller for unmatched model requests.
- Verify Banana response format with regression tests before replacing its container.
- Report recharge as unpaid until upstream payment/balance evidence exists.

## Tasks
1. Capture backups and apply reversible routing change.
2. Install missing tmpfiles rules and restart patrol API and patrol.
3. Inspect Banana source, implement and verify response handling.
4. Trace repeated default-group requests, adjust only owned monitoring configuration if identified.
5. Verify post-change traffic and preserve evidence; hand over payment if login/payment is blocked.
