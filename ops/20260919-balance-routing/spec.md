# Balance-first routing for core text and image models

User instruction: use previously configured funded providers first, then rank by low cost, observed stability and response time. Scope is GPT-5.5, GPT-5.6 Sol/Terra/Luna, GPT-Image-2, Banana Flash/Pro. Do not alter any video channel, video gateway, Topaz, or GPT-6 configuration.

Plan: obtain current provider metadata and balances; test exact model aliases on funded candidates; prefer measured successful routes and compare costs at the same model and resolution; apply scoped channel/ability changes transactionally with backup. Retain existing sales prices. Toonflow G-2.0 is explicitly confirmed by the owner as GPT-Image-2. Screenshot pricing is 1K0.10,2K0.20,4K0.25 CNY/image and must be labeled screenshot evidence until authenticated billing is available.

No public-model availability or balance will be invented when an upstream requires renewed login. Preserve verified routes and report exact external blocker with alternatives.
