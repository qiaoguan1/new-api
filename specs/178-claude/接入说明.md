# Claude 接入与数据库连接池修复

更新日期：2026-09-26。型号名称是上游提供的接口标签，不作为官方型号真实性证明。

## 调用方式

- 地址：`https://api.aixingtuyun.com/v1`
- 标准接口：`POST /chat/completions`
- 认证：`Authorization: Bearer <用户自己的密钥>`
- 使用自动分组或文字分组；账号状态、余额、IP限制和模型白名单仍生效。
- OpenAI兼容非流式已验证8个型号；流式已验证Sonnet5。Claude原生`/v1/messages`仅对Opus5做过一次成功验证，不表示所有型号/工具调用已逐一验证。

## 型号、路由与售价

金额单位：人民币/百万Token。输入、输出、缓存读写分别取本型号保留渠道的最高有效成本，再乘1.5，不混用不同版本。

| model | 首选 | 备用 | 输入 | 输出 | 缓存读取 | 缓存写入 |
|---|---|---|---:|---:|---:|---:|
| claude-haiku-4-5-20251001 | Code Plan | 无 | 0.9 | 4.5 | 0.09 | 1.125 |
| claude-sonnet-4-6 | Paisio | Code Plan | 2.7 | 13.5 | 0.81 | 3.375 |
| claude-sonnet-5 | Paisio | 无 | 0.81 | 4.05 | 0.81 | 0.81 |
| claude-opus-4-6 | Paisio | Code Plan | 4.5 | 22.5 | 0.45 | 5.625 |
| claude-opus-4-7 | Paisio | Code Plan | 4.5 | 22.5 | 0.45 | 5.625 |
| claude-opus-4-8 | Paisio | 无 | 1.35 | 6.75 | 1.35 | 1.35 |
| claude-opus-5 | Code Plan | 无 | 4.5 | 22.5 | 0.45 | 5.625 |
| claude-opus-5-5 | Code Plan | 无 | 3.6 | 18 | 0.18 | 4.5 |

Maolao直连测试有成功，但完整调用与流式测试中3个型号发生长等待，本轮不加入Claude生产路由；其原有渠道不动。Paisio的Haiku目录存在但实际404，不接入。Jojo不恢复。视频、图片配置不改。

注意：Code Plan部分OpenAI兼容请求计入了约4千至7千上游输入Token，明显大于测试短提示词；小样本还出现输出超过max_tokens的情况。按上游实际返回usage计量，不把短提示词视为实际全部计费输入。不保证单次请求绝对费用上限。

## GPT6与分组边界

GPT6仍由Code Plan优先、海纳备用，`gpt-6`映射`gpt-6-astra`。正常自动分组密钥已能看到并路由。新建空分组密钥存在与文字组倍率不一致风险，此项需要用户单独确认，本轮未改默认分组、用户余额、白名单。

## 运维

- 主服务连接池持久化为：最大连接30、空闲5、连接生命周期300秒。
- PostgreSQL总连接数仍100，数据库未重启。
- 临时测试服务连接池5/1/60；测试账户独立，不使用客户余额。
- 配置和数据备份：服务器`/opt/ai-api-stack/backups/claude178-20260926`，仅管理员可读。
- 上游生产密钥保留服务器IP与精确型号白名单，未充值；Maolao临时测试密钥禁用。
- 故障回退只禁用本次Claude178渠道和模型元数据，不覆盖其他渠道或整份价格配置。
