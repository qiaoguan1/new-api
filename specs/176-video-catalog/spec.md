# Public catalog synchronization

Follow-up to173: API availability did not reach the model square. Add a read-only projection to /api/pricing through the existing frontdoor, retain every native row and personalized group metadata, and append only currently enabled, price-verified Nody video specs. The native pricing numeric convention is quota units divided by quota_per_unit and group ratio; explicitly include CNY fixed-spec reservation metadata and POST /v1/videos, not a chat endpoint. No database price/channel/wallet mutations.

Checklist: reproduce public native catalog missing seven; tests first; implement additive projection with bounded requests and graceful video-source degradation; review; backup/build/stage; switch only public frontdoor and /api/pricing route; compare all original rows and nine public model IDs; publish evidence. No paid generation.

Separately, user explicitly approved token24 q1 (lian123) SAN→auto. Completed with group-only compare, same key digest/limits/quotas, GET /v1/models200 and seven new videos. Private snapshot /opt/ai-api-stack/backups/lian123-catalog-20260925/token24-before.json contains no full key. Do not broaden this authorization to other tokens.
