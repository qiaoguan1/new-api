# 两项P1修复结果

2026-09-26 22:14生产切换，后续验证完成。服务器156.239.3.210，站点api.aixingtuyun.com。

## 1. 默认分组：已修复

- 启用系统已有DefaultUseAutoGroup开关；API创建空/省略group时统一保存auto，注册默认密钥路径同样生效。
- 完整编辑空分组按同一默认处理；仅启停密钥不会顺带改组。
- 显式指定文、图、default或其他非空分组保持原值，不擅自替换用户明确选择。
- 生产迁移62枚有效且未删除的空分组密钥。事务中校验除了group，所有字段均未改变；最终有效空分组为0。
- 没有改密钥字符串、账户余额、额度、状态、IP白名单、模型白名单、有效期。
- 实际新建API密钥验证：省略group后保存auto，受限目录包含GPT6与Claude；临时验证密钥随后删除，未提交付费生成。
- 抽样迁移密钥目录返回8个Claude及gpt-6、gpt-6-astra。账号状态、余额和白名单限制仍保留。

## 2. 自动改价与监控：程序修复已生效，数据覆盖仍为部分完成

- 精确识别4个站内HTTP适配器，限制渠道ID、原地址、目录路径和Docker桥接地址；没有放开任意HTTP/内网地址或重定向。
- 生产只读扫描24条启用渠道目录：24成功、0失败。目录可读不等于每个模型生成均成功，本轮没有付费测试。
- 修复gpt-6→gpt-6-astra账单映射；被禁用的模型能力不再误算为仍需覆盖的路由。
- 一个渠道异常不会中止整份扫描；一个型号成本错误不会阻止其他型号核价；日报可带明确缺失项继续生成。
- 保留所有启用路由的成本覆盖要求。来源不明或缺成本时，不能忽略较贵渠道而调低价格。
- “无需变化”不再计为写入；干运行不再冒充生产完成；blocked/partial/unknown/空评估不再误报健康。
- 原有Claude专用上游分组与缓存价格保留，禁止误用账户default目录价格覆盖。视频和图片任务配置未改。

### 本轮生产真实执行

| 项目 | 结果 |
|---|---|
| 业务日 | 2026-09-25 |
| 评估条目 | 55 |
| 实际价格写入 | 0 |
| 有证据、核验后无需调整 | 2 |
| 跳过 | 53，其中35项为保护的视频/放大型号 |
| 业务状态 | partial，巡检warning，而非全绿 |

核验通过的两个精确SKU：gpt-image-2.5-flare、gpt-image-2.5-sunburst，成本0.30元/次，现售价0.45元/次，符合×1.5，不需要人为制造改价记录。

### 仍需补齐的资料或映射

- 海纳：用户人工提供的人民币换算、型号和缓存计费覆盖。
- Toonflow：具体分辨率对应的完整实际成本证据。
- 寒鹤：GPT5.5、Image2.5近期可信成本。
- Banana：适配器底层实际来源及成本映射。
- Claude：专用密钥分组和缓存维度的每日证据接入；当前已验证售价继续保留。

这些项目不属于“程序执行失败”，但也不能宣称已全部自动改价。缺证据型号保持原价，不猜测、不补扣、不退款、不擅自删渠道。后续跟踪：GitHub issue181。

## 验证与安全

- 186项Python测试通过；Go默认分组回归测试在本地和生产基础构建环境通过。
- 隔离环境验证省略/空分组、显式文/default、注册默认密钥路径。生产目录与新建密钥验证通过。
- 全部价格、缓存比例及分组倍率配置与变更前一致。
- 主服务镜像new-api-fixed:issue180-token-default；保留数据库权威扣费和30最大/5空闲连接池；数据库未重启，未再发现连接耗尽。
- 日报强制干运行成功，包含7家渠道汇总；没有重新发送当天已发日报。
- 本次隔离容器、克隆数据库及含测试凭据的临时监控目录已删除。生产验证临时密钥已删除。

备份及迁移证据仅保留服务器管理员目录：`/opt/ai-api-stack/backups/issue180-p1-20260926`。精确生产源码与脚本：`/opt/ai-api-stack/releases/issue180-p1`。没有在文档中记录密钥。

## 后续可选路径

A. 按现有最高保留成本×1.5规则补充缺失的精确证据，再核价。

B. 用户明确批准调整相关型号的上游保留范围后，仅对新的完整成本集合定价；会影响备用能力，不能自动替用户决定。

C. 继续当前安全模式：已核实的自动处理，缺证据保留原价并报告；不影响日报发送。
