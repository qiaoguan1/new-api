# NodyHub requested model onboarding

User scope: enable NodyHub and red-box image/video models only. Preserve other provider policies/routes and old prices. Payment callback URLs must not be replayed.

Requested images: grok-imagine-image-2.0, gpt-image-2.5-flare, gpt-image-2.5-sunburst.
Requested videos: wan3.0-video, wan3.0-video-prime, flux-3-video, grok-video-3, grok-imagine-1.5-video, grok-imagine-video-official, omni-flash.

Verified account1065 has10.011176 credit units. Authenticated recharge records show15CNY paid for10 units consistently, therefore cost conversion1.5CNY/unit, not prior configured1.0. Reusing existing active default-group token1711 in private storage; never expose it.

Current token catalog includes7/10 requested names, missing wan3.0-video/grok-imagine-1.5-video/grok-imagine-video-official. Catalog presence alone is not generation verification.

Acceptance: exact API schema/cost evidence; bounded representative live tests without exhausting balance; strict model/spec gates; async video billing/polling correctness; per-model enabled/blocked status; atomic backed-up configuration; protected old routes unchanged; user-facing integration report.

Tasks: [x] issue/project; [x] login/balance/conversion/catalog; [x] two synchronous image models contract/tests/deployment/end-to-end; [ ] remaining8 models pending upstream protocol/catalog evidence. See接入结果.md for verified partial delivery and exact limits.
