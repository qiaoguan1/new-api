# NodyHub 部分接入结果（2026-09-23）

## 已启用并验证

本站图片渠道57：NodyHub · Image2.5 Flare/Sunburst。

| 调用模型名 | 已验证规格 | 实际成本 | 对外售价 |
|---|---|---:|---:|
| `gpt-image-2.5-flare` | 1024×1024，单张文生图 | 0.30元/张 | 0.45元/张 |
| `gpt-image-2.5-sunburst` | 1024×1024，单张文生图 | 0.30元/张 | 0.45元/张 |

依据：认证充值记录15元支付/10额度到账；两次上游真实消费各100000 quota÷500000=.2额度，再×1.5兑换=.30元成本。本站按成本×1.5销售，图分组.15对应基础ModelPrice3。

```http
POST https://api.aixingtuyun.com/v1/images/generations
Authorization: Bearer <本站图分组API密钥>
Content-Type: application/json
```
```json
{"model":"gpt-image-2.5-flare","prompt":"一只蓝色杯子，白色背景","size":"1024x1024","n":1,"response_format":"b64_json"}
```

Sunburst仅需替换model。初版不开放2K/4K、其他比例、多图、参考图、编辑或高品质参数，避免未经核验的费用或规格被当成已支持。原有gpt-image-2.5及Image2/Banana/视频价格和渠道不变。

## 未启用项目

- Grok Imagine Image2.0：上游明确要求异步图片任务，当前size请求拒绝，缺可靠创建/状态查询/结果协议。
- Wan3.0、Grok Imagine1.5 Video、Grok Imagine Video官方：广场有，但当前有效默认通道令牌目录未列出；不能据此直接认定可调用或封禁。
- Wan3.0 Prime、FLUX3 Video、GrokVideo3、OmniFlash：目录有，广场有按秒价/范围，但仍缺具体请求字段、规格计费、同步/异步和查询协议，未发布、未付费生成。

官方文档入口来自NodyHub公开状态接口：https://lcn3ojq2jq5t.feishu.cn/wiki/BdVJwkbWPiUSggkyHoicSXoknfc 。网络读取受HTTP429限制；内置浏览器打开后读取超时，未能取得协议内容。

继续所需：上述型号的官方API调用示例、任务状态查询示例，以及分辨率/时长/声音/参考素材对应的完整价格表。可提供文档导出或复制对应API章节；不要只提供网页模型下拉框。

## 验证与恢复

- 上游Flare29.33s、Sunburst47.38s成功；本站端到端Flare32.08s、Sunburst53.06s成功，均225000本站quota=.45元。临时令牌671已撤销。
- 四项本地/服务器回归通过，认证和非法非JSON请求不会到达上游；参数白名单防止未验证模型/规格绕过。
- 新容器xtai-nodyhub-image-adapter健康，无公开端口，内部8097，非root、只读、并发2。
- NodyHub采集enabled恢复true、rate修正1.5；其他提供商配置未有意改动。当前账号/密钥保留，不额外充值、不回放支付回调。
- 备份：/opt/ai-api-stack/backups/nodyhub171-20260923-150416，包含数据库、两份监控配置原件和SQL回滚。恢复监控配置须核对后备份文件，避免覆盖后续其他修改。
- 其余8款及新适配器的进一步监控整合保留在issue171，不能宣称所有要求完成。
