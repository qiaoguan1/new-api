# NodyHub 新密钥测试（2026-09-23）

后续更新：用户再次充值后，Omni Flash与FLUX补充测试均成功；详细新结果见同目录《补充测试结果.md》。本文保留首轮测试经过。

用户明确授权：新建密钥测试剩余型号；最多使用现有余额，不额外充值。

## 测试密钥

- 新建令牌2553，24小时到期，只允许156.239.3.210使用，有限额度；不输出密钥值。
- 初始8模型白名单导致Nody任务查询接口按空模型拒绝403。保留IP、到期及额度限制，临时取消模型白名单；所有测试脚本仍固定模型列表。
- 初始账户9.211176额度≈13.816764元；测试令牌上限9.201176额度，留0.01额度安全余量。未充值。

## 已生成并验证MP4链接可读

| 模型 | 成功请求入口 | 测试请求规格 | Nody账单额度 | 成本人民币 |
|---|---|---|---:|---:|
| Wan3.0 | `/v2/videos/generations` | duration2，resolution480p，aspect_ratio16:9 | 0.50 | 0.75 |
| Wan3.0 Prime | `/v2/videos/generations` | duration2，resolution480p，aspect_ratio16:9 | 0.72 | 1.08 |
| Grok Imagine1.5 Video | `/v2/videos/generations` | duration6，resolution480p，aspect_ratio16:9 | 0.60 | 0.90 |
| Grok Video3 | `/v1/videos` | seconds字符串6，size1280x720 | 0.60 | 0.90 |
| Grok Imagine Video官方 | `/v2/videos/generations` | duration1，resolution480p，aspect_ratio16:9 | 0.30 | 0.45 |

这是测试请求参数，不等于逐帧确认了输出实际分辨率/时长；已确认任务completed，五个结果URL均HTTP200、Content-Type video/mp4。未下载视频内容。

查询使用`GET /v1/videos/{网关返回UUID}`。不同上游返回结果格式不统一：Wan和Grok1.5在result.videos[0].url数组，Grok3在video_url，官方Grok在metadata.video_url。必须保留提交时的网关UUID，不用查询结果中出现的原始上游task_替换它。

目录未列出不等于实际不能调用：Wan3.0、Grok1.5、官方Grok已通过上述接口实测成功。

## 未通过/待选择

- Grok Imagine Image2.0：`POST /v1/images/generations`去掉size后返回submitted，任务号`task_01M373ZZNB7QND2Q9ZQ7NF9MXD`，扣.45额度=.675元。没有返回可用图片；常见查询路径404/找不到任务，后台任务列表也未找到。没有重复提交，需上游确认查询方法或退款。
- Omni Flash：2秒请求失败，明确只支持4/6/8/10秒；失败预扣退款。最低4秒测试至少2.16额度≈3.24元，尚待用户选择。
- FLUX3Video：尚未提交付费生成；5秒720p广场估算4.75额度≈7.125元，尚待用户选择。

## 已解决的测试错误

- `/v1/videos`用于Wan、Grok1.5及官方Grok时上游URL不兼容，改用`/v2/videos/generations`后成功。
- Grok3不接受854×480形成的427:240比例；改为1280×720。
- Grok3和Grok1.5最少6秒；1秒均失败并退款后才提交6秒测试。
- 缺prompt的v2验证请求也曾返回任务，但立即失败退.001额度；不能把v2响应queued当作完成。

本轮净消耗3.17额度≈4.755元（其中Grok图片未闭环.675元）；测试额度剩6.031176≈9.046764元。FLUX与Omni最低测试合计至少10.365元，超过剩余额度，已询问优先项并停止新支出。

本轮没有新增下游视频路由，没有改其他生产模型价格。原Flare/Sunburst两款图片仍保持已上线状态。
