# Plan

1. Change source-level evidence selection to recent actual first, then current
   authenticated catalog, then bounded manual authenticated-catalog evidence.
2. Add deterministic tests for precedence, expiry, units, and maximum selection.
3. Record the approved production route matrix and normalized costs.
   For `gpt-image-2`, preserve the 1K/2K/4K source table and prove whether the
   resulting per-resolution maxima differ before using a single fixed price.
4. Back up production channel and pricing state.
5. Apply the route priorities/model restrictions and computed prices in one
   reviewed maintenance transaction.
6. Re-read database state, run the pricing worker dry-run, and complete ten
   no-charge health checks.
